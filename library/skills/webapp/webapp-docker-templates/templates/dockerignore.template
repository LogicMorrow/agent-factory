# {{PROJECT_NAME}} — .dockerignore (zasada #15 pkt 1)
# Generated by skill webapp-docker-templates v1.0.0
#
# Cel: wykluczyć pliki niepotrzebne w obrazie Docker + zapobiec secret leak
# Zasada: .env* wykluczyć domyślnie, TYLKO .env.example dopuścić
# Reguły: precedencja ostatniej pasującej reguły (negacja ! działa)

# ─── Sekrety i konfiguracja lokalna (KRYTYCZNE — secret leak prevention) ─────
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
# Zezwól na przykładowy plik (nie zawiera sekretów)
!.env.example
secrets/
*.pem
*.key
*.p12
*.pfx
*.crt

# ─── Node.js ──────────────────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# ─── Next.js build cache ──────────────────────────────────────────────────────
.next/
# Nie wykluczaj .next/standalone — kopiowany explicite w Dockerfile stage runner
# (ale COPY w Dockerfile jest z --from=builder, nie z kontekstu budowania)
out/

# ─── Git ──────────────────────────────────────────────────────────────────────
.git/
.gitignore
.gitattributes
.github/

# ─── IDE i edytory ────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ─── Dokumentacja (zbędna w obrazie) ─────────────────────────────────────────
README.md
CHANGELOG.md
LICENSE
docs/
*.md
# Zezwól na package.json (potrzebny przez Dockerfile)
!package.json

# ─── Testy i pokrycie ─────────────────────────────────────────────────────────
__tests__/
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/*.spec.tsx
coverage/
playwright-report/
test-results/
.nyc_output/

# ─── Docker (nie kopiuj do własnego obrazu) ───────────────────────────────────
Dockerfile*
compose*.yml
compose*.yaml
.dockerignore

# ─── CI/CD ────────────────────────────────────────────────────────────────────
.github/

# ─── Artefakty i logi ─────────────────────────────────────────────────────────
artifacts/
*.log
logs/
tmp/
temp/

# ─── Tailwind + PostCSS cache ─────────────────────────────────────────────────
.tailwind-cache/

# ─── TypeScript (cache) ───────────────────────────────────────────────────────
tsconfig.tsbuildinfo
